actions:
  - name: "Deploy"
    triggers:
      push:
        branches:
          - "main"
      pull_request:
        branches:
          - "*"
    bazel_commands:
      - "run image:auth --config=workflows"
      - "run image:deploy --config=workflows"
