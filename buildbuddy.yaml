actions:
  - name: "Deploy"
    triggers:
      push:
        branches:
          - "main"
    bazel_commands:
      - "run image:auth --config=workflows"
      - "run image:deploy --config=workflows"
